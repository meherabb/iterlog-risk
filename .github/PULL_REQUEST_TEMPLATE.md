## What does this change?

## Which paper table/figure/theorem is affected (if any)?

## Checklist
- [ ] `pytest` passes locally
- [ ] `ruff check .` and `black --check .` pass locally
- [ ] Docstrings on any new/changed `src/bands/` or `src/baselines/` function cite the
      relevant theorem/corollary/equation
- [ ] If a reported number changes, `docs/REPRODUCING.md` and/or `docs/CROSSWALK.md` are
      updated to match
- [ ] No author-identifying information added (see `ANONYMITY.md`)
